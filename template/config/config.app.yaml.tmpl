# Command-specific configuration for app
# This file is merged with config.yaml when running the app command.
# Values here override the base configuration.

# Example overrides:
# app:
#   name: app-service
#
# logger:
#   level: debug  # More verbose logging for this command
#
# Add VPKG package overrides here after installing packages
